{
  "name": "gemini-mcp",
  "version": "1.0.0",
  "description": "Personal ChatGPT to Gemini Remote MCP",
  "scripts": {
    "test": "vitest run",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "tsc --noEmit",
    "build": "wrangler deploy --dry-run",
    "pretest": "wrangler deploy --dry-run --outdir dist",
    "deploy:local": "wrangler deploy --config wrangler.local.jsonc",
    "test:cli": "node --test tests/cli-script.test.mjs"
  },
  "dependencies": {
    "@cloudflare/workers-oauth-provider": "^0.10.3",
    "@google/genai": "^2.23.0",
    "@modelcontextprotocol/server": "^2.0.0",
    "agents": "^0.23.0",
    "jose": "^6.2.12",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260918.1",
    "miniflare": "^5.20260917.0-alpha",
    "typescript": "^7.0.2",
    "vitest": "^5.0.1",
    "wrangler": "^4.134.0"
  },
  "type": "module",
  "private": true
}
